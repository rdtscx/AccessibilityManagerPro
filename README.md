# 无障碍管理器 Pro (Accessibility Manager Pro)

基于 Android 无障碍服务的多功能管理器，支持服务保活、应用保活、通知调控、开发者选项直改、权限管理等功能。

## 关于本版本

- 不联网。清单文件里没有声明网络权限，App 物理上就不上不了网；不放心的可以拿 `aapt dump permissions` 自己查。
- 没有广告，也没接任何统计、崩溃上报、推送之类的 SDK。
- 不收集数据，所有配置和日志都只存在本机。
- 代码全部开源（MIT），想看看、想自己编译都行。
